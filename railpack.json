{
  "$schema": "https://railpack.io/railpack.schema.json",
  "build": {
    "preInstall": [
      "apt-get update && apt-get install -y libicu-dev",
      "docker-php-ext-install intl",
      "docker-php-ext-enable intl"
    ]
  }
}